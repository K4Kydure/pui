- Git files are modrinth mrpack format by default to avoid large filesize.
- To allow others to touch Entities/Vehicles in your claim:
```/openpac player-config set claims.protection.entitiesFromPlayers 0```
- To allow allies to touch blocks in claim: 
```/openpac player-config set claims.protection.fromAllyParties 0```
